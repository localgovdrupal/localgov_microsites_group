langcode: en
status: true
dependencies:
  config:
    - group.type.microsite
    - node.type.localgov_event
  module:
    - gnode
    - node
id: group_relationship_type_5a3c3717
label: 'Event'
description: 'An event to appear in the events listing and optionally integrate into a directory.'
group_type: microsite
content_plugin: 'group_node:localgov_event'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
