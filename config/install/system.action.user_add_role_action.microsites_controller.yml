langcode: en
status: true
dependencies:
  config:
    - user.role.microsites_controller
  module:
    - user
id: user_add_role_action.microsites_controller
label: 'Add the Microsites controller role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: microsites_controller
